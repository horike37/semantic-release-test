language: node_js

matrix:
  include:
    - node_js: '8.9'

sudo: false

install:
  - travis_retry npm install

jobs:
  include:
    - stage: release
      node_js: lts/*
      deploy:
        provider: script
        skip_cleanup: true
        script:
          - npx semantic-release
